# 英語學習護照 v2 — 設定與部署指南

這個版本在原本的單字/句型遊戲上，加了帳號系統、學習紀錄與老師後台。因為原本的專案是「純前端、沒有後端伺服器」的單一 `index.html`，這個新版本延續同樣的精神——用 **Firebase**（Google 提供的免費雲端服務）當「後端」，仍然只需要把一個 `index.html` 丟到 GitHub Pages 就能運作，不用自己架伺服器。

## 這次改了什麼

- **學生登入更簡單**：學生選班級、選座號、自己設一組密碼（至少6碼）就能開始用，**第一次登入時系統會自動用那組密碼幫他建立帳號**，不用老師先手動建號。之後同一個座號要用同一組密碼才能再登入。登入狀態會記在這台裝置上，之後打開網站會直接是登入狀態，不用每次重打密碼。
- **班級已經先設定好**：預設有「三年級、四年級、五年級、六年級」四個班級可以選，登入畫面座號是**下拉選單**（不是自己打字），選項數量對應各班實際人數（三年級5人、四年級2人、五年級5人、六年級6人），這樣同一個學生每次選到的座號一定完全一致，不會因為打成「3」「03」而被系統誤認成不同人；老師後台手動新增學生時的座號欄位也是同一份下拉選單。
- **老師登入**：老師固定就是**一蓁、逸蓁、士頡**三位，登入時直接點選名字、輸入共用密碼 `1646276077` 即可，不用個別註冊。
- **四階段闖關**：每個學習主題（例如 Grade 3 的 "Name 名字"）現在分成 **聽得懂 → 看得懂 → 排得出來 → 拼得出來** 四關，一定要照順序解鎖：
  - 聽得懂：只放發音、不給文字，選出正確中文意思（4選1）。
  - 看得懂：只給英文文字，選出正確中文意思（4選1）。
  - 排得出來：單字→把打散的字母排回正確拼法；片語/句子→把打散的單字排回正確順序。
  - 拼得出來：**只考單字**，不看提示、自己把英文單字完整打出來；句子不用整句一字不漏打出來(含標點)，句子只要「聽得懂、看得懂、排得出順序」就算學會了，不會被要求手打整句英文。
  - 原本的「配對遊戲」保留下來，變成不影響進度、隨時可以玩的自由練習。
- **字母練習 ABC（主要給三年級用）**：主畫面新增一個獨立的字母練習區，跟年級課程分開，兩個小遊戲：
  - **字母卡**：26 個字母，點字母聽名稱發音（例如唸出「A」），點例字（Apple 🍎）聽單字發音，幫助建立字母跟例字之間的連結。
  - **大小寫配對遊戲**：練習大寫 A 配小寫 a。
  - 小提醒：瀏覽器的語音功能沒辦法唸出單獨的注音式音標（例如 A 的 /æ/ 音），所以這裡做的是「字母名稱 + 例字發音」，這也是坊間自然發音教材常見的教法。
- **學習紀錄（任務）**：每個主題就是一個「任務」，學生每過一關系統就記錄下來；四關都過了，這個任務就算「完成」，等老師蓋章。
- **老師蓋章 = 點數**：學生完成任務後，老師後台會把它列在「待蓋章」，老師點一下「🖋️ 蓋章 +5」，該任務就蓋章認證、學生自動 +5 點。老師也可以用「其他加點」欄位手動加點/扣點並附原因（例如課堂表現）。學生自己沒有辦法核發點數，所有點數都是老師蓋章或手動給的，不怕被竄改。
- **老師後台**：管理班級、看每個學生有哪些任務待蓋章、已蓋章幾個、目前總點數；也保留手動新增學生名冊的功能，當作備用方式。
- **老師可以幫學生重置密碼**：每位學生名字旁邊有個「🔑 重置密碼」按鈕，老師輸入一組新密碼確認後，這個學生下次登入就要用新密碼，原本的進度和點數都會完整保留。重置後畫面會顯示新密碼，記得直接告訴學生。
- **學生「我的學習護照」**：學生登入後點主畫面的「我的學習護照」，可以看到自己在每個年級每個主題的四關進度、哪些任務已經蓋章認證（護照風格的印章樣式），還有目前累積的總點數。

## 第一步：建立 Firebase 專案

1. 前往 https://console.firebase.google.com ，用學校或你自己的 Google 帳號登入，點「新增專案」，輸入專案名稱（例如 `nypspassport`），一路下一步即可（不需要開 Google Analytics）。
2. 左側選單「建構 (Build) → Authentication」，點「開始使用」，在「Sign-in method」分頁啟用 **電子郵件/密碼 (Email/Password)**。
3. 左側選單「建構 (Build) → Firestore Database」，點「建立資料庫」，選「以正式版模式啟動 (production mode)」，地區建議選離台灣近的（例如 `asia-east1`）。
4. 左側選單「專案設定 (齒輪圖示) → 一般」，捲到最下面「你的應用程式」，點網頁圖示 `</>` 新增一個網頁應用程式（不需要勾 Firebase Hosting），註冊後會看到一段 `firebaseConfig = {...}` 設定值，把它複製起來。

> 如果你已經建立好專案了，這一步可以跳過，把 `firebaseConfig` 貼到 `index.html` 就好（見下一步）。

## 第二步：把設定貼到程式碼裡

打開 `index.html`，找到最前面這一段（`<script type="text/babel">` 開頭沒多久）的 `firebaseConfig`，換成你自己專案的值，存檔即可。

## 第三步：設定 Firestore 安全規則

1. Firebase Console → Firestore Database → 「規則 (Rules)」分頁。
2. 把這個資料夾裡 `firestore.rules` 的內容整份複製貼上，取代原本的內容，按「發布」。
3. 這份規則做的事：學生可以自己建立帳號（第一次登入時）、只能讀寫自己的 progress 進度、完全不能自己寫點數；只有一蓁/逸蓁/士頡這三個帳號能被建立成老師、只有老師能蓋章、加點；班級清單公開可讀（登入畫面下拉選單要用）。詳細說明都寫在規則檔的註解裡。

## 第四步：老師登入、學生自己登入

1. 部署好網站後（見下方「部署」），打開網站選「我是老師」→ 點選自己的名字（一蓁／逸蓁／士頡）→ 輸入共用密碼 `1646276077` → 登入。**第一次**用某個名字登入就會自動建立那個人的帳號，之後同一個名字要用同一組密碼才能再登入。
2. 學生的部分不用老師先準備名冊，請學生自己在「我是學生」畫面：選班級（三/四/五/六年級）→ 輸入座號 → **自己設一組至少 6 碼的密碼** → 填自己的名字（只有第一次要填）→ 按登入，系統就會自動建立他的帳號。之後同一個座號要用同一組密碼才能再登入，所以請提醒學生把密碼記好（可以寫在聯絡簿上）。
3. 如果老師想先幫學生把帳號建好（例如低年級不方便自己操作），後台也保留「+ 新增班級」「+ 新增學生」的功能，一樣是輸入座號、姓名、密碼（至少 6 碼），效果跟學生自己登入建立帳號是一樣的。
4. 學生四關全過之後，該任務會自動出現在老師後台「待蓋章」，老師點「🖋️ 蓋章 +5」即可。

## 部署到 GitHub Pages

這個 repo（`nypsscaa/nypspassport`）如果本來就是用 GitHub Pages 從 `main` 分支的根目錄發布，那麼：

1. 把改好的 `index.html` 覆蓋掉 repo 裡原本的 `index.html`，commit、push 到 `main`。
2. 等 1-2 分鐘讓 GitHub Pages 重新部署，再打開網站確認可以正常登入。

## 已知限制（請務必知道）

- **共用密碼是寫死在前端程式碼裡的**（`1646276077`），任何人打開瀏覽器的開發者工具都看得到。安全規則有另外擋一層（只有一蓁/逸蓁/士頡這三個帳號可以被建立），所以就算有人知道密碼，也沒辦法冒充新的老師身分，最多只能嘗試登入這三個既有帳號——所以密碼還是建議不要公開分享到班級群組以外的地方，如果要換密碼，同時要改 `index.html` 的 `TEACHER_SHARED_PASSWORD` 常數。
- **老師能看到「所有」班級的學生資料**，目前沒有做到「老師只能看自己班」的權限切分，因為就只有三位老師使用，先簡化處理。
- 點數完全由老師核發（蓋章 +5 或手動加點/扣點），學生端沒有任何寫入點數的權限，所以不用擔心被竄改點數。
- 目前的「任務」是**以主題（單元）為單位**（例如 Grade 3 的 "Name 名字"），不是每個單字獨立紀錄；蓋章也是整個任務蓋一次章。
- **學生忘記密碼**：老師後台可以直接「🔑 重置密碼」，不用擔心救不回來。技術上是這樣做的：因為 Firebase 沒辦法讓老師直接「修改」別人的密碼（也沒有真的 email 信箱可以收重設信），所以重置的實際做法是幫學生開一組新帳號、把姓名/進度/點數紀錄都搬過去，並且更新系統裡「這個座號現在對應哪個帳號」的指向資料，學生下次用同一個班級+座號登入時就會自動改用新帳號比對密碼，感覺起來就像單純換了密碼一樣。舊帳號本身沒辦法真的被刪除，但重置後不會再被使用到，不會造成任何影響。

## 之後可以再加的功能（供參考，這次先不做）

- 匯入 Excel/Google 表單的學生名冊，一次建立整班帳號，不用一個個手動新增。
- 班級排行榜、每週學習報告自動寄給老師或家長。
- 每個單字獨立紀錄進度（目前是每個主題/單元一組）。
- 老師專屬班級權限切分。
